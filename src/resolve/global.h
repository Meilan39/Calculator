#ifndef GLOBAL
#define GLOBAL

#include "../tree/tree.h"

const long double GLOBAL_E;
const long double GLOBAL_PI;

int numerical_print(Node* head);
int symbolic_print(Node* head);

int numerical_exception(Node* head);

#endif 